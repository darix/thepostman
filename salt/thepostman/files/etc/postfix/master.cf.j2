#
# Postfix master process configuration file.  For details on the format
# of the file, see the master(5) manual page (command: "man 5 master" or
# on-line: https://www.postfix.org/master.5.html).
#
# Do not forget to execute "postfix reload" after editing this file.
#
# ================================================================================
# service       type       private unpriv  chroot  wakeup  maxproc command + args
#                          (yes)   (yes)   (no)    (never) (100)
# ================================================================================
{%- for service, service_settings in config.items() %}
{{ "{:<15}".format(service) }} {{ "{:<10}".format(service_settings["type"]) }} {{ "{:<7}".format(service_settings["private"]) }} {{ "{:<7}".format(service_settings["unpriv"]) }} {{ "{:<7}".format(service_settings["chroot"]) }} {{ "{:<7}".format(service_settings["wakeup"]) }} {{ "{:<7}".format(service_settings["maxproc"]) }} {{ service_settings["command"] }}{%- if "args" in service_settings %} {{ service_settings["args"] }}{%- endif %}
{%- endfor %}
